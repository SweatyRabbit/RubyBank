<p> Hello World! </p>

<%= link_to 'Log out', destroy_user_session_path, data: { turbo_method: :delete } %>

<%= link_to 'My Account', accounts_path %>

<%= form_with(url: transactions_path, method: "post", local: true) do |form| %>
  <div class="field">
    <%= form.label :sender_card, "Select Sender Card" %>
    <%= form.select :sender_card, @sender_cards.collect { |card| card.number }, prompt: "Select a Card", required: true %>
  </div>

  <div class="field">
    <%= form.label :recipient_card, "Select Recipient Card" %>
    <%= form.select :recipient_card, @recipient_cards.collect { |card| card.number }, prompt: "Select a Card", required: true %>
  </div>

  <div class="field">
    <%= form.label :amount, "Amount" %>
    <%= form.text_field :amount, required: true %>
  </div>

  <div class="actions">
    <%= form.submit "Transfer Money" %>
  </div>
<% end %>
