<% if sent_transactions.present? || received_transactions.present? %>
  <h2> <%= t('transaction.user_transactions') %> </h2>
  <% sent_transactions.each do |transaction| %>
    <p> <%= t('transaction.sender_card', number: transaction.sender_card.number ) %></p>
    <p> <%= t('transaction.recipient_card', number: transaction.recipient_card.number) %></p>
    <p> <%= t('transaction.amount', amount: number_to_currency(transaction.amount / 100.0)) %></p>
    <p> <%= t('transaction.date', date: I18n.l(transaction.created_at)) %> </p>
  <% end %>

  <% received_transactions.each do |transaction| %>
    <p> <%= t('transaction.sender_card', number: transaction.sender_card.number ) %></p>
    <p> <%= t('transaction.recipient_card', number: transaction.recipient_card.number) %></p>
    <p> <%= t('transaction.amount', amount: number_to_currency(transaction.amount / 100.0)) %></p>
    <p> <%= t('transaction.date', date: I18n.l(transaction.created_at)) %> </p>
  <% end %>
<% else %>
  <p> <%= t('transaction.no_transactions') %> </p>
<% end %>
