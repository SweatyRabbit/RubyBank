<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%= link_to t('title'), root_path, class: "navbar-brand" %>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('links.home_page'), root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('links.user_account'), accounts_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('links.log_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('devise.log_in'), new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('buttons.sign_up'), new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>
